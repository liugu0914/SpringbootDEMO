<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="base">
	<select id="getbaselist" resultType="com.jiopeel.enter.bean.Base">
    	SELECT
			t_login.pid,
			t_login.loginnums,
			t_login.password,
			t_login.username,
			t_login.addtime,
			t_login.phone,
			t_login.email,
			t_login.rdmark
		FROM t_login
		WHERE 1=1
		and t_login.rdmark!='d'
	</select>
	
	<insert id="add_login" parameterType="com.jiopeel.enter.bean.Base" useGeneratedKeys="true" keyProperty="pid">
		INSERT INTO t_login (loginnums, password, username, addtime, phone, email, rdmark) 
		VALUES 
		(
			#{loginnums},#{password},#{username},#{addtime},#{phone},#{email},#{rdmark}
		);
	</insert>
</mapper>
